pre-commit:
  parallel: true
  commands:
    tsc:
      run: yarn typecheck
    build:
      run: yarn build
    lint:
      run: echo "TODO"
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: yarn commitlint --edit
