---
sidebar_position: 1
---

# Button

Button component with Pressable as container, and supports full customization with different props.
It has both hover and press effect that get picked from the color shades or if custom color is provided it auto generates those colors.

import {
  BlossomUIRenderer,
  PropsTable,
  ButtonUsage,
  ButtonStatuses,
  ButtonSizes,
  ButtonModes,
  ButtonDisabled,
  ButtonLoading,
  ButtonIcons,
  ButtonModesSizes,
  ButtonCustom,
} from "@site/src/components";

## Usage

<BlossomUIRenderer
  componentName={"ButtonUsage"}
  children={<ButtonUsage />}
  fileName={"Button"}
/>

## Status

<BlossomUIRenderer
  componentName={"ButtonStatuses"}
  children={<ButtonStatuses />}
  fileName={"Button"}
/>

## Size

<BlossomUIRenderer
  componentName={"ButtonSizes"}
  children={<ButtonSizes />}
  fileName={"Button"}
/>

## Modes

<BlossomUIRenderer
  componentName={"ButtonModes"}
  children={<ButtonModes />}
  fileName={"Button"}
/>

## Disabled

<BlossomUIRenderer
  componentName={"ButtonDisabled"}
  children={<ButtonDisabled />}
  fileName={"Button"}
/>

## Loading

When `isLoading` enabled, Button doesn't show the hover & press effect same as the disabled state to indicate that user can't do any action here.
The loading icon is set to left by default.
The loader color is set to same as the text color by default, it can be changed with `loaderProps`.

<BlossomUIRenderer
  componentName={"ButtonLoading"}
  children={<ButtonLoading />}
  fileName={"Button"}
/>

## Icons

<BlossomUIRenderer
  description={
    "You can use both the left/right props for left/right icons, it comes with default standard padding."
  }
  componentName={"ButtonIcons"}
  children={<ButtonIcons />}
  fileName={"Button"}
/>

## Modes and Sizes

<BlossomUIRenderer
  componentName={"ButtonModesSizes"}
  children={<ButtonModesSizes />}
  fileName={"Button"}
  withFlex={false}
  withCodeblock={false}
/>

## Custom Buttons

<BlossomUIRenderer
  componentName={"ButtonCustom"}
  children={<ButtonCustom />}
  fileName={"Button"}
/>

## Props

<PropsTable componentName="Button" />
