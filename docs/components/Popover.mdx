---
sidebar_position: 1
---

# Popover

Component to display content in a floating container relative to a target element.  
Supports custom positioning, offsets and target references.

import {
  BlossomUIRenderer,
  PropsTable,
  PopoverUsage,
  PopoverPosition,
  PopoverOffset,
  PopoverFitTargetWidth,
  PopoverTargetRefUsage,
} from "@site/src/components";

## Usage

<BlossomUIRenderer
  componentName={"PopoverUsage"}
  children={<PopoverUsage />}
  fileName={"Popover"}
/>

## Position

Use the `position` prop to control where the popover appears relative to its target (e.g., top, bottom, left, right).

<BlossomUIRenderer
  componentName={"PopoverPosition"}
  children={<PopoverPosition />}
  fileName={"Popover"}
/>

## PopoverOffset

Use the `offset` prop to adjust the distance between the popover and its target element.

<BlossomUIRenderer
  componentName={"PopoverOffset"}
  children={<PopoverOffset />}
  fileName={"Popover"}
/>

## Ref Usage

Use `targetRef` to attach the popover to a specific element programmatically.

<BlossomUIRenderer
  componentName={"PopoverTargetRefUsage"}
  children={<PopoverTargetRefUsage />}
  fileName={"Popover"}
/>

## Fit target width

Enable `fitToTargetWidth` to automatically match the popover width to the target element’s width.

<BlossomUIRenderer
  componentName={"PopoverFitTargetWidth"}
  children={<PopoverFitTargetWidth />}
  fileName={"Popover"}
/>

## Props

<PropsTable componentName="Popover" />
