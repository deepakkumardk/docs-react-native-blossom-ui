---
sidebar_position: 1
---

export const COMPONENT_NAME = "ProgressBar";

# ProgressBar

Component to visually represent progress or completion.  
Supports determinate and indeterminate states, directional control, sizing, and status-based colors.

import {
  BlossomUIRenderer,
  PropsTable,
  ProgressBarUsage,
  ProgressBarIndeterminate,
  ProgressBarDirection,
  ProgressBarStatuses,
  ProgressBarSizes,
  ProgressBarCustom,
  BlossomComponentPlayground,
} from "@site/src/components";

### Usage Playground

<BlossomComponentPlayground componentName={COMPONENT_NAME} />

## Usage

<BlossomUIRenderer
  componentName={"ProgressBarUsage"}
  children={<ProgressBarUsage />}
  fileName={"ProgressBar"}
/>

## Indeterminate

Enable `indeterminate` prop to show a continuous loading animation without a fixed progress value.

<BlossomUIRenderer
  componentName={"ProgressBarIndeterminate"}
  children={<ProgressBarIndeterminate />}
  fileName={"ProgressBar"}
/>

## Direction

Use the `reverseDirection` prop to reverse the progress bar i.e. it will start from right by default.

<BlossomUIRenderer
  componentName={"ProgressBarDirection"}
  children={<ProgressBarDirection />}
  fileName={"ProgressBar"}
/>

## Status

<BlossomUIRenderer
  componentName={"ProgressBarStatuses"}
  children={<ProgressBarStatuses />}
  fileName={"ProgressBar"}
/>

## Sizes

<BlossomUIRenderer
  componentName={"ProgressBarSizes"}
  children={<ProgressBarSizes />}
  fileName={"ProgressBar"}
/>

## Custom

<BlossomUIRenderer
  componentName={"ProgressBarCustom"}
  children={<ProgressBarCustom />}
  fileName={"ProgressBar"}
/>

## Props

<PropsTable componentName="ProgressBar" />
