---
sidebar_position: 1
---

export const COMPONENT_NAME = "Chip";

# Chip

Chip component with Pressable as container, and supports full customization with different props.
It has both hover and press effect that get picked from the color shades or if custom color is provided it auto generates those colors.

Here, below all the usage are shown with `withCheckIcon` prop set to true via the component manager, to get this as a default prop.

import {
  BlossomUIRenderer,
  PropsTable,
  ChipUsage,
  ChipViewOnly,
  ChipClearable,
  ChipCheckIcon,
  ChipWithoutCheckIcon,
  ChipAsBadge,
  ChipModes,
  ChipSizes,
  ChipDisabled,
  ChipModesSizesMobile,
  ChipAllStatuses,
  ChipCustom,
  BlossomComponentPlayground,
} from "@site/src/components";

### Usage Playground

<BlossomComponentPlayground componentName={COMPONENT_NAME} />

## Usage

<BlossomUIRenderer
  componentName={"ChipUsage"}
  children={<ChipUsage />}
  fileName={"Chip"}
/>

## View Only

Displays non-interactive chips for informational or read-only use cases.

<BlossomUIRenderer
  componentName={"ChipViewOnly"}
  children={<ChipViewOnly />}
  fileName={"Chip"}
/>

## Hide check icon

use `withCheckIcon` to show or hide the check icon

<BlossomUIRenderer
  componentName={"ChipWithoutCheckIcon"}
  children={<ChipWithoutCheckIcon />}
  fileName={"Chip"}
/>

## clearable

use `clearable` prop to show/hide the clear icon at right

<BlossomUIRenderer
  componentName={"ChipClearable"}
  children={<ChipClearable />}
  fileName={"Chip"}
/>

## With Check Icon

use your desired icon for left with `checkIcon` as a ReactNode

<BlossomUIRenderer
  componentName={"ChipCheckIcon"}
  children={<ChipCheckIcon />}
  fileName={"Chip"}
/>

## As Badge

Displays the chip styled as a badge for compact labeling or status display.

<BlossomUIRenderer
  componentName={"ChipAsBadge"}
  children={<ChipAsBadge />}
  fileName={"Chip"}
/>

## Size

<BlossomUIRenderer
  componentName={"ChipSizes"}
  children={<ChipSizes />}
  fileName={"Chip"}
/>

## Modes

<BlossomUIRenderer
  componentName={"ChipModes"}
  children={<ChipModes />}
  fileName={"Chip"}
/>

## Disabled

<BlossomUIRenderer
  componentName={"ChipDisabled"}
  children={<ChipDisabled />}
  fileName={"Chip"}
/>

## Modes and Sizes

Showcases how chip modes and sizes appear

<BlossomUIRenderer
  componentName={"ChipModesSizesMobile"}
  children={<ChipModesSizesMobile />}
  fileName={"Chip"}
  withFlex={false}
  withCodeblock={false}
/>

## All Statuses

<BlossomUIRenderer
  componentName={"ChipAllStatuses"}
  children={<ChipAllStatuses />}
  fileName={"Chip"}
  withFlex={false}
  withCodeblock={false}
/>

## Custom Chips

<BlossomUIRenderer
  componentName={"ChipCustom"}
  children={<ChipCustom />}
  fileName={"Chip"}
/>

## Props

<PropsTable componentName="Chip" />
