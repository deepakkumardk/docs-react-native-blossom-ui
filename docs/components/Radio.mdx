---
sidebar_position: 1
---

export const COMPONENT_NAME = "Radio";

# Radio

Component for selecting a single option.
Supports custom sizes, statuses, positions, default values, and full styling customization.

import {
  BlossomUIRenderer,
  PropsTable,
  RadioUsage,
  RadioStatuses,
  RadioSizes,
  RadioDisabled,
  RadioDefaultValue,
  RadioPositions,
  RadioCustom,
  BlossomComponentPlayground,
} from "@site/src/components";

### Usage Playground

<BlossomComponentPlayground componentName={COMPONENT_NAME} />

## Usage

<BlossomUIRenderer
  componentName={"RadioUsage"}
  children={<RadioUsage />}
  fileName={"Radio"}
/>

## Status

<BlossomUIRenderer
  componentName={"RadioStatuses"}
  children={<RadioStatuses />}
  fileName={"Radio"}
/>

## Size

<BlossomUIRenderer
  componentName={"RadioSizes"}
  children={<RadioSizes />}
  fileName={"Radio"}
/>

## Disabled

Use the `disabled` prop to make radio buttons non-interactive.

<BlossomUIRenderer
  componentName={"RadioDisabled"}
  children={<RadioDisabled />}
  fileName={"Radio"}
/>

## Positions

Use the `position` & `adjacent` prop together to place the label on the left or right of the radio button & spacing between them.

<BlossomUIRenderer
  componentName={"RadioPositions"}
  children={<RadioPositions />}
  fileName={"Radio"}
/>

## Default Value

Use `defaultValue` to preselect a radio button when the component mounts.

<BlossomUIRenderer
  componentName={"RadioDefaultValue"}
  children={<RadioDefaultValue />}
  fileName={"Radio"}
/>

## Custom

<BlossomUIRenderer
  componentName={"RadioCustom"}
  children={<RadioCustom />}
  fileName={"Radio"}
  withFlex={false}
  withCodeblock={false}
/>

## Props

<PropsTable componentName="Radio" />
