---
sidebar_position: 2
---

export const COMPONENT_NAME = "DatePicker";

# DatePicker

A versatile DatePicker component with a popover calendar view for selecting dates.  
It supports custom formats, default values, and restrictions like minimum, maximum, or disabled dates.

import {
  BlossomUIRenderer,
  PropsTable,
  DatePickerUsage,
  DatePickerClearable,
  DatePickerDisplayFormat,
  DatePickerDefaultValue,
  DatePickerMaxDate,
  DatePickerMinDate,
  DatePickerMinMaxDate,
  DatePickerDisableDates,
  BlossomComponentPlayground,
} from "@site/src/components";

### Usage Playground

<BlossomComponentPlayground componentName={COMPONENT_NAME} />

## Usage

<BlossomUIRenderer
  componentName={"DatePickerUsage"}
  children={<DatePickerUsage />}
  fileName={"DatePicker"}
/>

## Clearable

Use the `clearable` prop to allow resetting the selected date.

<BlossomUIRenderer
  componentName={"DatePickerClearable"}
  children={<DatePickerClearable />}
  fileName={"DatePicker"}
/>

## Display Format

Use the `displayDateFormat` prop to format the displayed date, following the format options from the [dayjs library](https://day.js.org/docs/en/parse/string-format#list-of-all-available-parsing-tokens).

<BlossomUIRenderer
  componentName={"DatePickerDisplayFormat"}
  children={<DatePickerDisplayFormat />}
  fileName={"DatePicker"}
/>

## Default Value

Provide a default value using either a `Date` object or a formatted string (matching the `outputDateFormat`).

> **Note:** Always use `outputDateFormat` when passing string dates to the component.

<BlossomUIRenderer
  componentName={"DatePickerDefaultValue"}
  children={<DatePickerDefaultValue />}
  fileName={"DatePicker"}
/>

## Min Date

Restrict date selection to only those after a specific minimum date.

<BlossomUIRenderer
  componentName={"DatePickerMinDate"}
  children={<DatePickerMinDate />}
  fileName={"DatePicker"}
/>

## Max Date

Restrict date selection to only those before a specific maximum date.

<BlossomUIRenderer
  componentName={"DatePickerMaxDate"}
  children={<DatePickerMaxDate />}
  fileName={"DatePicker"}
/>

## Min & Max Date

Combine both `minDate` and `maxDate` to define a valid date range.

<BlossomUIRenderer
  componentName={"DatePickerMinMaxDate"}
  children={<DatePickerMinMaxDate />}
  fileName={"DatePicker"}
/>

## Disable Dates

Disable specific dates by passing an array of `Date` objects or formatted strings matching `outputDateFormat`.

For example, if `outputDateFormat` is `"DD-MM-YYYY"`,  
then `"10-02-2025"` should be passed in that exact format.

<BlossomUIRenderer
  componentName={"DatePickerDisableDates"}
  children={<DatePickerDisableDates />}
  fileName={"DatePicker"}
/>

Here, if you look closer the date string (`10-02-2025`) passed here is formatted in the same as of the `outputDateFormat` format i.e. `DD-MM-YYYY`

## Props

<PropsTable componentName="DatePicker" />
